# Скопируй в .env и заполни. ЗАПУСК.md — шаг 2.
# База из docker-compose.dev.yml слушает на 5433 (не 5432!)
DATABASE_URL="postgresql://dastarhan:dastarhan@localhost:5433/dastarhan?schema=public"
# Любая случайная строка от 32 символов. Обязательно поменяй на проде.
JWT_SECRET=""
PORT=3000
