const historySummary=(r,g,b)=>`${r} продлений, ${g===0?'ни одной отсрочки':g+' отсрочка'}, ${b===0?'ни одной блокировки':b+' блокировка'}`;
let p=0,f=0;const eq=(n,g,w)=>{const o=g===w;o?(p++,console.log(`  ✓ ${n}`)):(f++,console.log(`  ✗ ${n}: ${g}`))};
eq('чистая история', historySummary(17,0,0), '17 продлений, ни одной отсрочки, ни одной блокировки');
eq('с отсрочкой', historySummary(17,1,0), '17 продлений, 1 отсрочка, ни одной блокировки');
eq('с блокировкой', historySummary(5,2,1), '5 продлений, 2 отсрочка, 1 блокировка');
console.log(`\nИТОГ: ${p}/${p+f}`); process.exit(f?1:0);
